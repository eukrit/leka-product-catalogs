# leka-product-catalogs — Project Index
_Last updated: 2026-04-22_

> Single-page project reference. Claude reads this + `.claude/PROGRESS.md` at session start. Update when structure changes.

## Status
- **Version:** [0.1.0] · **Last build:** 2026-04-22 · **Status:** [deployed|failed|pending] · **Commit:** [sha]
- **Branch:** [main|master]
- **GitHub Repo:** https://github.com/eukrit/leka-product-catalogs
- **Cloud Run URL:** [if any]
- **Live dashboard:** [if any]

## Directory Map
| Concern | Path in repo |
| --- | --- |
| Cloud Build config | `cloudbuild.yaml` |
| Dockerfile(s) | `Dockerfile`, `Dockerfile.python` |
| Manifest (services, APIs, env refs) | `manifest.json` (local only), `manifest.example.json` (committed) |
| Source code | `src/` |
| Scripts (utilities, deploy, sync) | `scripts/` |
| Tests | `tests/` |
| Docs (markdown, developer-facing) | `docs/` |
| Documents (user-facing generated HTML/PDF) | `documents/` |
| Skills (Claude-invocable skills this project defines) | `.claude/skills/` |
| Agents (Claude Code subagents for this project) | `agents/` |
| Session state (local) | `.claude/PROGRESS.md` |
| Plans / build plans | `docs/BuildPlans/` |
| Credentials (local only, gitignored) | `credentials/`, `.env` |

## Logs, Versions & Build Summaries
- [BUILD_LOG.md](BUILD_LOG.md) — full version history (semver, dated entries)
- [CHANGELOG.md](CHANGELOG.md) — if this project uses one instead of BUILD_LOG
- [docs/build-summary.html](docs/build-summary.html) — single-file rollup of every build/version with status badges and commit links. Regenerated by `scripts/generate-build-summary.sh` from BUILD_LOG.md on each build.
- [docs/DeploymentPlan.md](docs/DeploymentPlan.md) — task backlog and planned deploys
- [docs/BuildPlans/](docs/BuildPlans/) — one file per version

## User-Facing Dashboards & URLs
- Production URL: [...]
- Staging URL: [...]
- Internal dashboard: [...]
- GitHub Pages: [...]
- Notion dashboard page: [...]

## Credentials (references only — values in GCP Secret Manager)
| SM Secret Name | Env Var | Purpose |
| --- | --- | --- |
| [peak-user-token] | `PEAK_USER_TOKEN` | Peak API auth |

## Databases & Data Stores
- **Firestore** (database `leka-product-catalogs`, native mode, `asia-southeast1`):
  - Per-brand product collections: `products_wisdom`, `products_vinci`, `products_vortex`, `products_4soft`
  - Per-brand category lookup: `product_categories_{brand}`
  - **Shared (brand-null) categories** (since v2.17.0): `product_categories` — docs `epdm`, `infill`
  - **Shared product collections** (since v2.17.0): `products_epdm` (53), `products_infill` (5) — sourced from the EPDM 2024 Pricelist sheet, queryable by `cfh_m` for fall-height lookup.
  - `leka_vendor_quotations`
- **Google Sheets:** `EPDM 2024` (id `1wXGZoseE4PWEiY14BmtrYaHkkCJJEPyLQnUte7qUGrg`) — source of truth for pricelist; re-sync with `scripts/sync_epdm_pricelist.py`.
- **GCS bucket:** `ai-agents-go-documents` (product images, public read, uniform bucket-level access)

## CFH Lookup (for other projects)
Other projects (steel-fabrication, leka-projects, sales-orders, quotation tooling) can pick the cheapest EPDM that meets a required playground fall height by querying Firestore directly:
```python
from google.cloud import firestore
db = firestore.Client(project="ai-agents-go", database="leka-product-catalogs")
docs = (db.collection("products_epdm")
          .where("cfh_m", ">=", required_cfh_m)
          .where("status", "==", "active")
          .order_by("cfh_m")
          .order_by("pricing.quote_thb_per_sqm")
          .limit(1)
          .stream())
```
Fields available: `cfh_m` (m), `thickness_mm`, `sbr_mm`, `system`, `pricing.quote_thb_per_sqm` (THB/sqm), `pricing.landed_thb_per_sqm`, `description`, `item_code`. Composite indexes are deployed for `(status, cfh_m, pricing.quote_thb_per_sqm)` and `(system, cfh_m, thickness_mm)`.

## Scheduled / Cron / Cloud Scheduler
| Name | Schedule | Target | Code path |
| --- | --- | --- | --- |
| [daily_sync] | [06:00 daily] | [Cloud Run / Function] | [scripts/sync.py] |

## Cloud Build & CI/CD
- **Trigger:** `[trigger-name]` on `^main$`
- **Steps:** [test → build image → push to Artifact Registry → deploy to Cloud Run]
- **Artifact Registry:** `asia-southeast1-docker.pkg.dev/ai-agents-go/leka-product-catalogs`
- **Cloud Run service:** `[service-name]` · **Region:** `asia-southeast1`
- **Secret bindings (from cloudbuild.yaml `secretEnv`):** `[SECRET_ENV_VAR → projects/ai-agents-go/secrets/<name>/versions/latest]`

## Skills & Agents
- **Skills defined** (`.claude/skills/<name>/SKILL.md`): [none | list with purpose and trigger phrases]
- **Agents used:** [subagent types referenced, where]

## Connectors (external services — API-first per Rule 7)
### Slack
- `#[channel-name]` (`C01234ABCD`) — [purpose]
### Google Drive
- Folder `[name]` — [URL]
### Notion
- Page / Database — [URL]
### Google Sheets
- `[name]` — [URL]
### Gmail labels
- `project/[label]`
### Webhooks / n8n
- [URL]
### APIs & preferred paths
- [Service] ([API + MCP | API only | MCP only]) — prefer [API|MCP] in [scheduled jobs | interactive]

## Collaborators & Access
- Canonical list: [COLLABORATORS.md](COLLABORATORS.md)
- **Branch protection on `main`:** PR + 1 approval (owner) + CODEOWNERS review + status checks + no direct pushes + no force pushes.
- **CODEOWNERS:** [.github/CODEOWNERS](.github/CODEOWNERS)

## Security Surface
Read this before granting access to anyone.

| Item | Risk | Who must NOT have access |
| --- | --- | --- |
| [Service account with Domain-Wide Delegation] | **HIGH** (impersonates any Workspace user) | `secretmanager.secretAccessor` on its key; `iam.serviceAccountTokenCreator` on it |
| Cloud Build auto-deploy trigger | HIGH (push-to-main ships code) | Direct push to `main`; modify `cloudbuild.yaml` without review |
| [Hot secrets] | MEDIUM | `secretmanager.secretAccessor` unless scoped |
| Firestore write | MEDIUM | `datastore.user` / `datastore.owner` unless required |

See [SECURITY.md](SECURITY.md) for the full threat model and onboarding checklist.

## Relationships
- **Depends on:** [other projects]
- **Depended on by:** [other projects]
